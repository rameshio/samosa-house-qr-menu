{
  "name": "samosa-house-server",
  "version": "1.0.0",
  "description": "Backend API for Samosa House QR Menu",
  "main": "src/server.js",
  "type": "module",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "test": "node --test tests/**/*.test.js",
    "lint": "eslint ."
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.21.0"
  },
  "devDependencies": {
    "eslint": "^9.10.0",
    "supertest": "^7.0.0"
  }
}
